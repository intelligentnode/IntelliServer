{
  "rewrites": [
    { "source": "/api-docs/(.*)", "destination": "/api-docs/$1" },
    { "source": "/api/(.*)", "destination": "/api/$1" },
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}